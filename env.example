# ===========================================
# ENVIRONMENT CONFIGURATION
# ===========================================

# Use only when going live on VPS
# NODE_ENV=production
# PORT=3000
# DB_HOST=your_vps_db_host
# DB_USER=your_vps_db_user
# DB_PASSWORD=your_vps_db_password
# DB_NAME=your_vps_db_name
# DB_DIALECT=mysql
# JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random_123456789

# Use only when we are working on localhost
NODE_ENV=development
PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=my_gig_guide_db
DB_DIALECT=mysql
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random_123456789

# Optional: Restricted roles for ACL (comma-separated)
# RESTRICTED_ROLES=1,2,3
